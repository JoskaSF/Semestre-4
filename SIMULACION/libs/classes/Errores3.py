#!/usr/bin/env python

from enum importa enum

class Errores3(Enum):
    INTERVALO = "El intervalo no existe"
    PARAMETROT = "El valor del parametro t no puede ser negativo"
    CANTIDAD = "No se puede generar una cantidad negativa"
    DECIMALES = "No se puede redondear a una cantidad negativa"